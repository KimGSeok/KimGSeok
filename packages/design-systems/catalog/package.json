{
  "name": "@kimgseok/design-catalog",
  "version": "0.1.0",
  "description": "Machine-readable component catalog for the KimGSeok design system.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/KimGSeok/design-system.git",
    "directory": "packages/design-systems/catalog"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "files": ["dist"], "sideEffects": false,
  "type": "module",
  "exports": { ".": { "types": "./dist/registry.d.ts", "default": "./dist/registry.js" } },
  "scripts": { "build": "tsup src/registry.ts --format esm --dts --out-dir dist --clean", "typecheck": "tsc --noEmit", "test": "node --experimental-strip-types scripts/test.mjs" },
  "devDependencies": { "typescript": "^5.9.3" }
}
